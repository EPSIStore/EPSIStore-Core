package com.epsi.core.repositories;

import com.epsi.core.entities.Depend;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DependRepository extends JpaRepository<Depend, Integer> {
}
