package com.epsi.core.repositories;

import com.epsi.core.entities.Hierarchies;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HierarchiesRepository extends JpaRepository<Hierarchies, Integer> {
}
