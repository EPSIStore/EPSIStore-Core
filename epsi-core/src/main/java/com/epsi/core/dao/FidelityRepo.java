package com.epsi.core.dao;

import com.epsi.core.entities.Fidelity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FidelityRepo extends JpaRepository<Fidelity, Long> {
}
